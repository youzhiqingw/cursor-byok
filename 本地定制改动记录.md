# cursor-byok 本地定制改动记录（Go 版 v1.0.0 线）

> 仓库：`youzhiqingw/cursor-byok`（fork 自 `leookun/cursor-byok`）
> 记录日期：2026-09-26（本记录第二次整理）
> 许可证：**MIT**（Copyright 2026 leookun）—— 非商用自用、修改、再分发均合规，保留版权声明即可

---

## 1. 背景与记录口径

- **作者已迁移技术栈**：leookun 已把项目从 Go 迁移到 **Rust**（`upstream/main` 现为 Rust/Tauri 版：`server/` + `apps/desktop/` + `crates/`），Go 版最后维护线是 `upstream/archive/v0.0.49`。
- **本记录的对象**：你基于 **Go 版最后维护线** 的本地 fork（当前 `main`，应用版本 1.0.0）。后续你会**重新 fork Rust 版自用**，本记录用于防止遗忘你实际做过的改动。
- **记录口径**：只记录**你自己实际做的修改**（以修改原有代码为主，基本不新增功能）；合并上游带来的“作者新功能”单列一节，避免日后混淆。

**净变化**（相对 `upstream/archive/v0.0.49`）：40 个文件，+4047 / −1520 行。

---

## 2. 你自己的修改（代码层面）

> 总原则：**云端编译**（GitHub Actions 构建 Windows 包），本地只改原有代码，基本不新增功能。实际落地改动只有三块代码 + 一套构建发布体系。

### 2.1 去广告（核心改动，约 −1100 行）

| 文件 | 改动 |
| --- | --- |
| `internal/ads/http.go` / `service.go` / `system.go` / `types.go` | 整个广告模块删除（约 992 行，含广告拉取/展示/上报） |
| `internal/bridge/ad.go` | 广告桥接绑定删除（约 45 行） |
| `internal/app/runner.go` | 移除广告服务初始化、3 分钟后台刷新循环、窗口聚焦刷新、广告事件注册 |
| `internal/backend/host.go` | 移除广告路由挂载 `server.Mount(ads.RoutePrefix, …)` |
| `internal/appdata/paths.go` | 移除 `AdsRootPath()` |
| `frontend/src/services/clientApi.js` | 广告 API 置空 |
| `frontend/src/layouts/MainLayout.vue` | 移除“作者 leookun”页脚按钮及加载逻辑 |
| `internal/bridge/window_author.go` | 作者主页入口停用（不再打开 bilibili 链接） |

关键代码（前端广告 API 置空）：

```js
export function getAdRuntime() {
  return Promise.resolve(null);
}

export function openAdExternalURL(_url) {
  return Promise.resolve();
}
```

> 说明：协议层保留 `InAppAdService` 404 路由以兼容旧协议，不实际提供服务。

### 2.2 Tab 外发开关：具体化并自控

把原有的 tab 流量外发行为，改成一个**由你控制的明确开关**：默认关闭，关闭时相关路由返回 404，不向外部发送 Tab 上下文 / 文件 / git 数据；需要时手动开启，且可自定义端点。

配置结构（`internal/backend/server/config/types.go`）：

```go
// DefaultTabServerBaseURL 是 tab 外发默认端点，仅在用户开启 Tab 外发时生效。
DefaultTabServerBaseURL = "https://tab.leokun.cn"

type TabServerConfig struct {
	Enabled bool   `json:"enabled" yaml:"enabled"`
	BaseURL string `json:"baseURL,omitempty" yaml:"baseURL,omitempty"`
}
```

路由开关（`internal/backend/host.go`，未开启时返回 404）：

```go
func tabServerProcedure(pattern string, name string, protocol server.RouteOption, deps upstream.Dependencies) server.Option {
	if !deps.TabServerEnabled {
		return server.POST(pattern,
			server.Name(name),
			protocol,
			server.Local(func(ctx *server.Context) error {
				http.NotFound(ctx.Writer, ctx.Request)
				return nil
			}),
		)
	}
	// 开启时：转发到 deps.TabServerBaseURL（默认 tab.leokun.cn）
	...
}
```

配套改动：
- `internal/backend/server/upstream/types.go`：依赖注入 `TabServerBaseURL` / `TabServerEnabled`
- `internal/backend/server/config/store.go`：配置持久化判定支持 `tabServer` 键
- `frontend/src/views/Home.vue`：首页新增“Tab 外发”Switch（提示“重启服务后生效”）
- `frontend/src/state/appState.js`：新增 `saveTabServer({ enabled, baseURL })`，保存失败自动回滚

### 2.3 per-installation CA 修复

修复旧版 embedded-CA 迁移后**证书存在但私钥缺失导致启动失败**的问题：自动重新生成证书对，并改用 per-installation CA 加载。

```go
// internal/certs/local_ca.go
// A certificate without its private key cannot sign MITM leaf certificates and
// is useless. Regenerate the pair. This covers migration from the legacy
// embedded-CA build, which persisted ca.crt for Cursor trust but never
// persisted ca.key.
if certErr == nil && errors.Is(keyErr, os.ErrNotExist) {
	return generateAndPersistManager(certPath, keyPath)
}
```

```go
// internal/app/runner.go
certManager, caCertPEM, err := certs.LoadOrCreateManager(appdata.CACertFilePath(), appdata.CAKeyFilePath())
```

同时新增 `internal/certs/ca.key`，`testdata/legacy_shared_ca.crt` 替换为 per-installation CA。

### 2.4 云端编译与发布（构建体系）

你主要在云端编译，这套 CI 是实际使用的基础设施：

| 改动 | 文件 |
| --- | --- |
| Windows 云端构建 Workflow（zip/exe/sha256/update.json，手动触发 + `v*` 标签自动发 Release） | `.github/workflows/build-windows.yml`（新增 243 行） |
| 打标签发版同步发布 GHCR 容器包 | `.github/workflows/build-windows.yml` + `build/docker/publish.Dockerfile` |
| Windows 平台 zip 改用 `Compress-Archive`（修复 runner zip 不支持 `-y`） | `build/windows/Taskfile.yml` |
| `create:zip` 去掉 `\$` 转义（go-task 经 sh 展开问题） | `Taskfile.yml` |
| 发布源 / 自动更新 URL 改指 `youzhiqingw/cursor-byok` | `internal/buildinfo/buildinfo.go`、`Taskfile.yml` |
| 版本号 `0.0.40 → 1.0.0` | `build/config.yml` |
| 忽略会话临时 npm 缓存 | `.gitignore`（`/.npm-tmp`） |

Workflow 关键约定（与 updater 自动更新兼容）：

```yaml
# 产物：zip / exe / sha256 / update.json
# 资产名：<base>-<ver>-windows-amd64.zip（与 updater 约定一致）
# 触发：workflow_dispatch（任意分支/标签）+ push v* 标签自动构建并发布 GitHub Release
# 仅作用于 youzhiqingw/cursor-byok
```

---

## 3. 文档说明（重要，避免误记）

仓库内出现的以下文档是在本地提交 `d7963ec` 中加入的，**并非上游自带**（Go 维护线 `archive/v0.0.49` 与 Rust 版 `upstream/main` 中均不存在）：

| 文档 | 性质 |
| --- | --- |
| `BUILD_WINDOWS.md` | 构建指南，与 2.4 云端编译配套，**实际可用** |
| `SDD.md` / `spec.md` / `plan.md` | 设计规格 / 开发计划类备忘 |
| `CLAUDE.md` / `VIBE_CODING.md` | AI 协作编码规范类备忘 |
| `PROJECT_REVIEW.md` / `SECURITY_AUDIT.md` | 评审 / 安全审计类备忘 |

> **注意**：据你的说明，这些文档中描述的部分功能**并未实际落地实现**（因为过于复杂），只作为思路备忘存在，**不要把它们当成已实现的功能**。重新 fork 后如需使用，以代码实际行为为准。

---

## 4. 上游合并进来的作者功能（非你新增）

以下功能出现在你的仓库里，是因为合并了上游（`archive/v0.0.49` 线），**不是你写的**，重新 fork 时直接来自上游即可：

| 功能 | 来源 |
| --- | --- |
| 完整配置导入导出（config transfer） | 上游 `3fc04ee`（Sxuan-Coder） |
| 图片读取按内容哈希持久化 | 上游 `8f8d288`（leookun） |
| 推理强度可设置为不指定 | 上游 `edd59dc`（Sxuan-Coder） |
| tool call 回放修复（文本与 call/result 交错时保留） | 上游 `5d04b5b` / `b7f8237` |
| 长耗时请求被误报 Network disconnected 修复、proto 打包修复 | 上游 `e9dc2e0` / `a90c8d9` |
| prompt 体系更新（agent/ask/debug/plan/multitask/subagent + tools.json） | 上游合并内容 |
| proto 更新（aiserver_v1.proto） | 上游合并内容 |

> 判断方法：这些提交的作者均为 leookun / 上玄 / 杨超 等上游作者，非你（youzhiqingw）的提交。

---

## 5. 提交历史（本地 10 个提交）

| 提交 | 日期 | 内容 | 归属 |
| --- | --- | --- | --- |
| `d7963ec` | 08-16 | 保存本地自定义修改：去广告前端/上报头、版本 1.0.0、本地文档 | 你的修改 |
| `f53e24c` | 08-16 | Tab 外发开关具体化、移除广告代码、per-installation CA 修复 | 你的修改 |
| `96fc95f` | 08-16 | 合并 upstream/main，保留本地修改 | 合并上游 |
| `7304d58` | 09-22 | 合并 upstream/archive/v0.0.49，保留本地去广告/Tab开关/CA 修改 | 合并上游 |
| `1e2f278` | 09-22 | Windows 云端构建 Workflow | 你的修改 |
| `b4c7701` | 09-22 | 源改指 fork、Workflow 对齐自动更新约定 | 你的修改 |
| `340911c` | 09-22 | Windows zip 改用 Compress-Archive | 你的修改 |
| `878f384` | 09-22 | create:zip 去掉 `\$` 转义 | 你的修改 |
| `ced0b76` | 09-22 | 打标签同步发布 GHCR 容器包 | 你的修改 |
| `ee87d91` | 09-22 | 忽略 `.npm-tmp` | 你的修改 |

---

## 6. 重新 fork Rust 版时的位置速查（路径仅供参考，以实际代码为准）

| 事项 | Rust 版（upstream/main）对应位置 |
| --- | --- |
| 广告（前端） | `apps/desktop/src/shell/ads/`（AdMenu.tsx、FloatingAd.tsx、cache.ts、types.ts） |
| 广告（后端控制） | `server/src/control/ads.rs` |
| Tab 设置界面 | `apps/desktop/src/features/settings/TabSettingsCard.tsx` |
| Tab 服务 | `server/src/cursor/services/tab.rs` |
| 本地 CA | `server/src/local_app/ca/`（mod.rs、windows.rs） |
| CI | `.github/workflows/ci.yml`、`release.yml` |
| 桌面壳（Tauri） | `apps/desktop/src-tauri/` |

> 提示：Rust 版同样带有广告模块，去广告与“外发控制”的思路可以按 Go 版的做法平移（删除/停用对应服务与 UI 入口，保留协议层 404 兼容）。

---

## 7. 附录：累计 diff 统计（`upstream/archive/v0.0.49..main`）

| 文件 | 变化 |
| --- | --- |
| `.github/workflows/build-windows.yml` | +243 |
| `internal/ads/http.go` / `service.go` / `system.go` / `types.go` | −84 / −773 / −64 / −71 |
| `internal/bridge/ad.go` | −45 |
| `internal/app/runner.go` | ±235（净删为主：广告服务移除 + CA 加载改造） |
| `internal/backend/host.go` | ±27（去广告路由 + Tab 开关） |
| `internal/backend/server/config/types.go` / `store.go` | +14 / +3 |
| `internal/backend/server/upstream/types.go` | +2 |
| `internal/certs/local_ca.go` / `ca.key` / `testdata/legacy_shared_ca.crt` | +7 / 新增 / 替换 |
| `internal/buildinfo/buildinfo.go` | ±6 |
| `frontend/src/**`（clientApi.js / MainLayout.vue / Home.vue / appState.js / i18n） | 约 ±400 |
| 本地文档（BUILD_WINDOWS / SDD / spec / plan / CLAUDE / VIBE_CODING / PROJECT_REVIEW / SECURITY_AUDIT） | 约 +3500 |

> 数据来源：`git log` 与 `git diff upstream/archive/v0.0.49..main` 实算；文档行数以 `d7963ec` 提交统计为准。
